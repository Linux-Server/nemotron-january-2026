# 1.4b — Phase-1 T1 gate completion (the single-stream behavioral-equivalence residual)

Project directory: /home/khkramer/src/nemotron-january-2026/proj-2026-05-24-from-scratch-runtime

## Goal
1.4a proved native COMPUTE-COMPOSITION equivalence (C++ session token-exact vs finalize_ref, 200 corpus streams + fork
isolation). The paired review (`reviews/session-FOLDED.md`) showed the FULL Phase-1 single-stream T1 behavioral gate is
NOT yet met. This phase closes the residual so the C++ single stream is behaviorally equivalent to the Python reference
on EMIT behavior, AUDIO input, MULTI-TURN continuous context, and corpus WER — not just cumulative tokens.

Oracle: `finalize_ref.py` is the validated executable spec (token-exact vs NeMo, 1.3a) and already contains the
emit/delta logic (`_continuous_append_only_delta`) and the speculative-vs-cold reset/resume. Use it (extended) as the
per-step oracle. A production-SERVER event-stream oracle is the strongest check but heavier (needs the running server);
it is an explicit follow-up (Step 6), not a blocker for the finalize_ref-spec equivalence.

## Rules
See PLAN_RULES.md. Every step: Codex first-pass (/cx-delegate) → I review every changed file → build+run in
`nemotron-aoti:cu128` → validate against the (extended) finalize_ref oracle → commit. Steps 1, 3, 5 are correctness-
critical → a PAIRED adversarial review (Codex + Opus) before marking done.

## Steps

- [x] **Step 1 — Event-stream / final-delta / suppression equivalence.** (SCOPE narrowed per `step1-event-FOLDED.md`:
  scheduler **generation-suppression** is an async-scheduler concept with no single-stream analog → deferred to Phase-2;
  Step 1 covers final-DELTA + empty/duplicate-final suppression + TEXT-based interim emission.) Port the emit logic to
  `cpp/session_main.cpp` at the **WORD/TEXT level** (the reference `_continuous_append_only_delta` is word-level — a
  token-level port is provably wrong on subword corrections, e.g. New→Newark): add an id→token-piece path so C++
  reconstructs surface text, computes the word-level append-only delta + text-change interim + empty/duplicate suppression,
  and compares the emitted TEXT (delta/collector/interim) against the exported text gold (the bundle already carries it).
  Interim oracle must be **AOTI-consistent** (gold steady tokens from the AOTI steady encoder, not eager) so the event
  check is pure-logic. Add synthetic NON-empty-collector unit cases (New→Newark, play→playing, duplicate, shortened final).
  Assert the full ordered TEXT event stream == gold (not just cumulative tokens). PAIRED REVIEW.

- [x] **Step 2 — Multi-turn continuous context-retention.** Add a multi-turn bundle mode to `export_session_bundle.py`:
  one stream = turn A audio → vad_stop → speculative finalize (context RETAINED) → turn B audio in the SAME session →
  next steady + 2nd finalize → true-boundary cold reset. C++ session: do NOT cold-reset between speculative turns; keep
  caches/decoder/collector across the speculative finalize (mirror finalize_ref `_finish_speculative`); cold-reset only on
  true boundary. Assert retained caches + collector text + per-turn deltas + cumulative tokens vs finalize_ref across the
  whole multi-turn session.

- [ ] **Step 3 — Audio-fed C++ front.** Replace the mel bundle's pre-baked mels with PCM: C++ runs the native
  preprocessor (audio→mel, the 0.8 `.ts` preproc) + maintains the raw-audio ring + assembles the finalize remainder from
  audio (recompute padded_total/total_mel/remaining_frames/T in C++ and assert vs Python). Feed PCM to the session;
  assert per-chunk mel hashes + final geometry vs Python, then re-run the Step 1/2 token+event assertions from audio.
  PAIRED REVIEW.

- [ ] **Step 4 — Full-1000 session WER + per-utterance bounds.** Run the C++ session over all 1000 stt-benchmark
  utterances; emit hypotheses; compute WER (whisper-normalized + jiwer) vs references; assert WER non-inferior to the
  established baseline within CI AND per-utterance WER bounded. Publish the sample list + normalizer + versions.

- [ ] **Step 5 — Hardening (paired-review MAJORs).** Native first chunk (AOTI first-chunk path or explicitly drop the
  "pure native" claim + report first-chunk drift/argmax-margin); AOTI steady output-ownership / recurrent-cache alias
  assertions + a long-stream cache-stability check; FORK_ASSERT scope (assert `mode`/state transitions + audio-ring
  counters in one long-lived session); adversarial coverage manifest (residual buckets / zero-many steady / corrections /
  duplicate finals / vad_start-cancel / stale-gen / cold reset). PAIRED REVIEW.

- [ ] **Step 6 — (follow-up) Production-server event-stream oracle.** Capture the actual server's emitted JSON events
  (NEMOTRON_CONTINUOUS=1) for the same audio scripts; compare the C++ event stream event-for-event vs the SERVER (not
  just finalize_ref). Closes the oracle-transitivity gap. May be deferred to Phase-2 entry if it needs significant server
  harnessing — flag as input-needed if so.

## Progress
| Step | Status | Commit | Notes |
|---|---|---|---|
| 1 event/delta/suppression | done | 30e0eb7 | correctness-critical, paired |
| 2 multi-turn continuous | done | 716e352 | |
| 3 audio-fed front | todo | | correctness-critical, paired |
| 4 full-1000 WER | todo | | the WER part of T1 |
| 5 hardening | todo | | paired |
| 6 server oracle | todo | | follow-up; may need input |
