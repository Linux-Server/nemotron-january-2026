# Paired red-team verdict — going-forward plan (Step 1c et seq.)

**Date:** 2026-05-27 | **Folds:** `opus-goforward-plan-review.md` + `codex-goforward-plan-review.md` (two independent red-teams, each tasked to challenge the plan it helped seed). **All load-bearing facts re-verified against the telemetry by the orchestrator (see "Verified facts").**

## VERDICT: **GO-WITH-CHANGES** (both reviewers, independently)

Directionally right to refuse a rubber-stamp of N=36, but it makes "push >36" load-bearing on (a) a noisy, not-apples-to-apples S_py, (b) an over-credited cheap lever, and (c) two quantitative claims that don't survive the telemetry. All fixable with re-sequencing + tighter pre-registered gates **before** spending on 1c. The two reviews **converged** on six must-fixes — high confidence.

## VERIFIED FACTS (orchestrator re-checked each cited number)

1. **S_py is noisy + non-monotonic, and the fold had a factual error.** spy ttfs_p95 by conc: 12→131, 14→58, 16→**147**, 18→67, 20→158, 24→**249 (first FAIL >175)**. CHECKPOINT/`opus-scaling-above-36-levers.md` said "level-16 fails 147ms" — **wrong, 147<175 = PASS.** With `repeats=2`, 1–2 samples set each p95 → S_py≈20 is a coin-flip near the 175 line, not a wall. **CORRECTED in CHECKPOINT.**
2. **DECISIVE — the decode-sync lever is bounded ~17ms and cannot close the decode_wall explosion.** `decode_item_wait` p95 is **flat 5.6 / 17.1 / 16.9 ms** (N=36/40/44) while `decode_wall` p95 **explodes 17 / 309 / 455 ms**. So the per-token `.item()` sync is a *small constant*; the explosion is the joint/predict GEMMs **queued under cross-stream contention** — which **batching** addresses, **sync-removal does not**. This **inverts the lever ranking** (Codex's batching-first over Opus's sync-first) and means **1c-0's expected outcome is PIVOT**. (`enc_len_sync` ~22ms + `decode_item_wait` ~17ms ≈ 39ms total host-sync — Tier-1a's whole ceiling.)
3. **The 44–48 util ceiling is an invalid extrapolation.** N=36 `gpu_util_mean=73%` but **p50=91, p95=96** (bimodal — 91–96% busy in active windows). And **staggered N=40 fails at 72.6% util** (*more* idle than N=36, yet collapses). Mean-util is not linear stream headroom; 44–48 is an optimistic upper bound, not a forecast.

## THE SIX CONVERGENT MUST-FIXES (both reviewers) — applied to PHASE2-PLAN.md

- **MF-1 — Lock S_py BEFORE 1c (the #1 fix).** S_py≈20 is noisy AND **not apples-to-apples**: Python ttfs is **client-over-WS** (`ec2_loadgen.py:77`), native N=36 is **server-side, pre-WS** (Step 3 unbuilt) → the 1.8× is inflated; the honest WS-paid multiplier is lower. Uncertainty is two-sided: S_py≤18 → bar 34, 36 clears, **1c unnecessary**; S_py≥22 → need native knee **≥43** (which 1c's own — over-estimated — ceiling barely reaches). → **New Step 1b.5 S_py_LOCK** (blocking): same Step-4 manifest, WS-matched, `repeats≥10`, p50/p95/p99, report a CI; set `S_native_req = ceil(max(34, 1.80·S_py_lock, 1.50·S_py_lock/0.83))`; ">36" is only a target if the formula says so.
- **MF-2 — Funding re-confirm checkpoint** (the multiplier softened from a hoped 2–2.25× to a zero-margin 1.8×, lower after the WS correction). → **New Funding-recheck F1** before the first big build spend: GO-to-build only if nominal `0.83·S_native/S_py ≥1.70×` AND pessimistic `0.75·… ≥1.50×`; else TECHNICAL-CONDITIONAL → human re-justification.
- **MF-3 — 1c-0 is not a clean arbiter.** 5090 is a dev proxy not an L40S density arbiter; `decode_wall` ≠ `.item()`-wait (FACT 2); device-argmax may not be T1-exact (the N=16 rerun already tripped a serial-oracle mismatch); the fill-window double-edge isn't gated. → **Split into 1c-0a (5090 dev/T1 smoke, NOT a density verdict) + 1c-0b (L40S net-density arbiter)** with a net-no-regression gate at N=36 and an AMBIGUOUS band (50–150ms → run 1c-A/1c-B). **Device-argmax T1-exactness is a fixture precondition;** if it fails, 1c-0 collapses to enc_len-only (~no-op) → straight to batching decision.
- **MF-4 — Downgrade the diagnosis to a hypothesis.** "Host-side serialization" is a working hypothesis, not attributed fact (the 2-runner finalize pool + steady contention + launch scheduling are unexcluded confounds; finalize_wait=0 is mis-attributed). → **1c-B Nsight is MANDATORY** before declaring batching necessary/unnecessary; soften "44–48 ceiling" to "upper bound; mean-util extrapolation invalid."
- **MF-5 — enc_first demotion is traffic-conditional.** Right for the harness's ~12s sessions, but real traffic (barge-in / reconnects / multi-stream-per-call / greeting snippets) re-stresses the first-chunk path. → keep de-prioritized **only if** target traffic has p95 session lifetime ≥60s OR first-chunk starts ≤5% of steady chunks at the cap; else a Step-2 hygiene gate.
- **MF-6 — Step-2 not fully blocked + a 1c STOP line.** Invariant scheduler/server work (admission/close-shed accounting, stale-gen harness, WS-tail microbench, telemetry schema) → **Step 2a, parallel after S_py_LOCK**; only the batched-vs-not topology → **Step 2b, waits on 1c-A**. And a **Step 1c STOP line**: stop when a knee clearing `S_native_req` is stagger-robust at N and N+4, OR after 1c-0+1c-B if no ≤Tier-1b lever closes the formula → escalate to F1, do NOT auto-continue into Tier-3/batching.

## THE DISAGREEMENT, RESOLVED BY FACT 2
The prior fold's open question (Opus: cheap sync-removal first; Codex: batching first) is **settled by the telemetry: batching is the decode-contention lever, sync-removal is bounded at ~17ms.** BUT batching is B-fill-gated (sim: B≈1.5–2 → 20–35%) and Phase-3-scale → the realistic path to >36 is **genuinely uncertain and possibly expensive**, which is *exactly* why **locking S_py first (MF-1) is the highest-value next move** — if S_py≤18, none of it is needed.

## WHAT THE PLAN GOT RIGHT
Marks Step-4 AT-RISK (not a comfortable pass); keeps T1/correctness binding before perf; separates technical-GO from funding-GO; flags the finalize_wait=0 mis-attribution; treats native B>1 batching as Phase-3-scale. The AOTI-exec-lock kill (`std::shared_lock`) and the Tier-1b "free finalize-runner" premise are code-verified.

## CAVEATS
Neither reviewer re-ran anything (committed logs only). The native exact knee is bracketed [36,39], not pinned. **The apples-to-apples p50/p95/p99 chart (native 16/20/24 + Python p99) is still in flight and SUPERSEDES every S_py≈20 statement** here and in the plan once it lands — it is effectively the first half of MF-1's S_py_LOCK.
