# 1.3b — finalize encoder substrate: exact-T per-T AOTI buckets + shared weights

> **⚠ STATUS (corrected by paired review `reviews/enc-scale-FOLDED.md`):** the per-T bucket MECHANISM is validated for the
> **observed drop=2, right-context=1 case** — token-exact 1000/1000 vs eager finalize_ref, shared-weights GPU-flat,
> strip token-exact. It is **NOT production-complete.** OPEN (blocking for production, fold into 1.4): **drop=0**
> first-finalize (T≈33..49) is untested + uncovered (only a T49 fixture bucket); **missing/out-of-range (drop,T) buckets
> silently PASS** in both the Python gate and C++ Phase B → lost-transcript hole → need fail-closed + an EAGER fallback;
> the **C++ loader path is only 4-row validated** (the 1000-shadow is Python); the **T-range assumes rc=1** (prompted
> models use rc=3); the two **strippers are inconsistent** + drop=0 strip unvalidated; artifact **metadata/contract**
> (model-id/rc/T-range/digest) is missing and the `e.*`/`encoder.*` weight alias is load-bearing with no identity check.
> Earlier phrases below ("production-proven", "not novel risk") are RETRACTED — the remaining items are real, not just
> packaging. (NeMo equivalence is also only canary-scale, not 1000-row.)

## Finding
Attempted a single dynamic-T `torch.export` of the finalize encoder step (`keep_all_outputs=True`, drop=2, T dynamic
20–320) so the C++ port would have one AOTI-compilable program for the variable remainder length (`export_finalize_t2a.py`).
**It fails** with `Constraints violated (T)`: the Conformer pre-encode does 8× subsampling (`//2` three times), so the
encoder output frame count is `1 + ((1 + ((1 + T//2)//2))//2)` — floor arithmetic that generates divisibility guards on T.
torch.export's own suggested fix is `T = 8*_T - 3` (a single residue class mod 8). Real finalize T values do NOT share one
class (idx2 T=45=8·6−3 ✓, but T=44/58 ✗; first-chunk T=261), so **one graph cannot cover arbitrary T** — the graph
structure genuinely depends on T mod 8.

## Decision: padded fixed-T buckets (mirror production)
This is exactly why production finalize used **fixed-T buckets with padding** (the finalize CUDA-graph: T_min=42, T_max=60;
memory `finalize-graph-WIN` / `deployment-target-sagemaker`: "padded finalize SHIPPED byte-exact"). The C++ finalize encoder
substrate should do the same:
- Export/AOTI-compile the finalize encoder at a FIXED chunk T per bucket (fixed-T export is byte-exact — same as the steady
  `export_t2a.py` at T=42/25), with the **valid length passed as a separate input** (so padding is masked, NOT derived from
  the padded chunk shape — the current `export_finalize_t2a.py` derives length internally, which only works for the no-pad case).
- At finalize: pad the remainder `chunk_mel` up to the next bucket T, pass the true `valid_length`, run the bucket graph,
  slice the output to `enc_len`. Common continuation finalize (drop=2) lands in T≈42–64; the rare drop=0 first-chunk
  (T up to ~261) needs a larger bucket or an eager fallback.
- Validate token-benign / WER-neutral exactly like the steady encoder (E.1/E.2): the padded-bucket output, decoded, must
  match the eager finalize tokens.

## Pad-to-single-bucket is ALSO out (not token-safe)
Tested (`test_finalize_padding.py`) padding the finalize chunk to a single fixed bucket T=64 + true length + slice, then
the FULL final-token continuation decode (fork decoder state) vs the unpadded exact-T encoder, over 40 drop=2 finalize
samples: **39/40 token-exact, 1 flip** (idx=18: 62→61, a deletion). The padding is NOT byte-transparent — the depthwise
conv right-context sees the trailing zeros, so the last valid frames drift (enc_out diff **up to 3.6e-1**, erratic). The
output caches are destroyed (diff 27–83) but those are discarded (speculative fork), so only enc_out matters — and at
~2.5% utterance flip rate it's far worse than the steady encoder's 1/1000. **Conclusion: finalize needs EXACT-T encoder
processing (no padding bleed).**

## RESOLVED: shared-weights per-T buckets (decision taken; mechanism PROVEN)
**The AOTI shared-weights path works.** Root cause of the E.3 cold-load segfault: AOTI constants-on-disk packages
(`package_constants_in_so=False, package_constants_on_disk=True`) build a tiny wrapper `.so` + the weights as separate
files, but **`aoti_load_package` does NOT auto-wire them** — `run()` then dereferences null → SIGSEGV. Fix:
`model.loader.load_constants(cmap, check_full_update=False, user_managed=True)` wires a supplied `{fqn: tensor}` map in;
`user_managed=True` means the tensors are SHARED (not copied). Validated (`validate_shared_weights.py`): all 637 FQNs
matched, run produced **max_abs_diff 1.663e-2 vs eager — IDENTICAL to constants-in-`.so`**. So:
- Per-exact-T finalize buckets = N tiny wrapper `.so` (one per T, fixed-T export is byte-exact, exact-T ⇒ no padding
  bleed ⇒ token-safe) + **ONE** shared 2.5 GB weight set loaded once and `load_constants`-shared into every bucket runner.
- FQNs are `e.<encoder_param>` (the export `Step` named the encoder `e`); the shared weights come from the encoder's
  named_parameters+buffers (`export_shared_weights.py` → `finalize_shared_weights.pt`), or one extracted set in production.

### 1.3b-enc-build DONE (fixture-T mechanism, fully validated)
Built the 4 buckets the fixture needs (drop0 T49, drop2 T44/45/58): `export_finalize_buckets.py` (host, fixed-shape
torch.export byte-exact) → `aot_compile_buckets.py` (container AOTI constants-on-disk + noexecstack). C++ Phase B
(`cpp/finalize_main.cpp`): load `finalize_shared_weights.ts` once → {fqn:tensor} CUDA map; per bucket
`load_constants(map, user_managed=true)`; route by exact (drop,T); run on contiguous cloned caches; decode + assert.
**Result: PhaseA+B PASS, all 4 rows token-exact vs finalize_ref AND nemo oracle, FORK_ASSERT PASS** (now meaningful — the
encoder runs on the cloned caches).
**GPU-RAM sharing MEASURED (the binding constraint):** loading 4 buckets each with `load_constants(user_managed=true)` on
the shared cmap → GPU alloc stays **2.48 GB (one weight set), +0.000 GB per additional bucket**. So N buckets cost ONE
weight set, not N → ~19 buckets ≈ 2.48 GB GPU (not 47 GB). The shared-weights architecture scales.
- **Remaining = scale-out engineering (not novel risk):** (a) build the full T-range buckets (drop2 T≈42-60 + drop0
  outliers) — just expand the T list, GPU-RAM proven flat; (b) strip dead in-package weights (each `.pt2` is 2.48 GB on
  DISK because constants-on-disk still packages the weight files, though they're never loaded — load_constants supplies
  them; stripping → ~1.2 MB/bucket); (c) route a real finalize chunk's (drop,T) to its bucket in the live session (1.4).

### (historical) build plan
1. Per-T export+AOTI: for each T in the finalize range (~42–60; drop=2 continuation) export the fixed-T finalize Step
   (keep_all_outputs=True, length-as-input or T-fixed), AOTI-compile constants-on-disk (noexecstack shim) → `enc_finalize_T{n}.pt2`.
   Handle the rare drop=0 first-chunk (large T) as its own bucket or eager.
2. C++ multi-bucket finalize encoder: load all bucket runners, `load_constants(shared_weights)` into each (shared), route a
   finalize chunk to its exact-T bucket, run, slice to enc_len. Wires Phase B of finalize_main.cpp + the real encoder-cache FORK_ASSERT.
3. Validate token-exact vs finalize_ref across the corpus (no bleed → expect clean), R2 paired review.

## (superseded) Earlier options considered for an EXACT-T finalize encoder in C++
1. **Per-exact-T AOTI buckets** (production-faithful: the finalize T range is naturally ~42–60 = pre(9)+remainder+padding;
   plus the rare drop=0 first-chunk outlier). Each fixed-T AOTI is byte-exact (export_t2a proved fixed-T byte-exact). BUT
   each package embeds ~2.5 GB of weights → ~19 packages is impractical UNLESS AOTI **shared weights / constants-on-disk**
   works (it segfaulted on a fresh load in the steady E.3 attempt — would need fixing).
2. **Eager finalize encoder** (finalize is off the hot path, once/turn, so it need not be AOTI-compiled). BUT "eager in C++"
   has no easy callable — cache_aware_stream_step isn't TorchScript-scriptable, and we have no eager conformer in C++. Would
   need either a scripted/exported eager path or a hybrid (native steady + Python/eager finalize), which dents "pure native".
3. **Hybrid**: native steady hot path + a thin eager/Python finalize service (pragmatic; finalize is rare).

## Status / scope
- This is a SEPARATE sub-component from the C++ finalize ORCHESTRATION (state machine + fork isolation + decode-continuation),
  which is validated against the fixture's precomputed eager `enc_out` and does NOT need this substrate (1.3b Phase A).
- `export_finalize_t2a.py` is retained as the record of the dynamic-export dead-end; the padded-bucket exporter is the
  follow-up (1.3b Phase B encoder). Not novel risk — production already runs this; it's implementation + the masking-padding
  byte-exactness check.
