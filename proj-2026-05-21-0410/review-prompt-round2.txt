<task>
REVIEW round 2 of 3 (do not implement) of the plan at proj-2026-05-21-0410/PLAN.md. It was already
revised once by folding round-1 reviews (your prior codex-review-round1.md + claude-review-round1.md,
both in proj-2026-05-21-0410/). Verify the round-1 findings were correctly incorporated AND find any
REMAINING or DEEPER issues. Round 2 should be sharper, not a rehash.

READ: proj-2026-05-21-0410/PLAN.md (current v2), codex-review-round1.md, claude-review-round1.md, and
re-check the real code (src/nemotron_speech/server.py: ASRSession ~364, _process_chunk ~2429, fork ~2068,
decoding config ~793, prompt ~639) + conformer_stream_step (mixins.py:592) + cache layout
(conformer_encoder.py:1087-1107) + the decoder factory (rnnt_decoding.py:384-455).

VERIFY the v2 plan correctly captures the round-1 critical fixes:
- Cache axis: channel/time concat on dim 1, len on dim 0 (NOT dim 0 for all). Is it stated correctly + with scatter `[:, i:i+1, …]`?
- Decoder: `strategy=greedy_batch` (not just loop_labels=True) gated behind a B=1 byte-exact Probe C.
- Flat per-row hypothesis list + unique objects + alias/permutation tests.
- Grouping key `(target_lang, keep_all_outputs, drop_extra, chunk_T, decoder_mode)`; no first+steady mixing; no padding not-ready streams; exact ready predicate.
- Step 0 baseline artifact; split scheduler 5a/5b/5c; fail-closed; latency gates; finalize B=1 serialized.

THEN find DEEPER issues the round-1 pass may have missed, e.g.:
- Are the GO/NO-GO gate THRESHOLDS concrete enough to act on autonomously (e.g. Probe A "≥20% faster" — measured how/where; Probe C "materially faster" — quantify)?
- Step DEPENDENCIES/ordering: does any step assume an artifact a later step produces? Is the compile×batch interaction matrix (Step 10) sufficient?
- Is keeping decode per-row while batching the encoder (the Probe-C-NO-GO fallback in Step 7) actually a throughput win, or does per-row decode re-serialize and negate the batch benefit? Quantify the expected ceiling in each branch.
- CUDA-graph (`mode=reduce-overhead`) + a variable batch size B are fundamentally incompatible (graphs need static shapes); does the plan correctly keep compile (Phase 1, fixed B=1 shape) and batching (variable B) as SEPARATE, or does Step 10's "both together" matrix have a latent static-shape conflict?
- Memory/OOM bounds for large B; the `NEMOTRON_BATCH_MAX_SIZE` default; backpressure when the ready-queue grows.
- Anything in the silence0_warm200 fork path (deep clone cost per finalize, FORK_ASSERT under the scheduler) that the plan still under-specifies.
- Is the plan now TOO big (12 steps)? Should any merge, or any probe be cut?

Be specific, cite file:line, propose concrete edits. WRITE to proj-2026-05-21-0410/codex-review-round2.md
(--write THIS FILE ONLY). Do NOT modify PLAN.md or any code. No commits.
</task>

<compact_output_contract>
Write proj-2026-05-21-0410/codex-review-round2.md: (1) round-1-fold verification (what's correct / still wrong), (2) deeper issues, (3) gate-threshold concreteness, (4) compile×batch static-shape conflict check, (5) prioritized concrete edits. Then a 5-line summary.
</compact_output_contract>
