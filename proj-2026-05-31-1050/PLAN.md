# Plan: Dispatch-front-end concurrency optimization (PHASE2-PLAN Tier-3/Tier-4 activation)

Project directory: `./proj-2026-05-31-1050`

_Revision history: **v5** — after Round 5 (Codex `b7dgrod3q` + Opus): **CONVERGED — only minor nits**, both reviews. Folded R5 nits: removed the stale "no-op behaviorally" phrase in 1b (the capacity change tightens admission timing — not a pure no-op); named the Step-2 zero-consumer retirement call sites explicitly (`:513` fault drain / `set_item_exception`, scheduler `close()`, DispatchResult RAII). No blockers/majors remain → READY to /implement. **v4** — after Round 4 (Codex `b1qnc2a7i` + Opus, CONVERGENT). R4 fixes: (BLOCKER) capacity-token retirement must be **progress-driving** — if tokens release only via `dispatch_done` query while the dispatcher idles in `gather_batch` (`:301`) and producers sleep on `cv_capacity_`, CUDA completion wakes nobody → stall (exists even in 1b since `dispatch_done` is post-unpack/clones); fix = drain/query pending dispatches in the enqueue predicate + before any dispatcher idle wait + timed poll / small reaper + a shutdown drain on `close()` (never an indefinite `cv_`/`cv_capacity_` wait while all capacity is pending on CUDA completion); (MAJOR) the 1b "no-op behaviorally" claim weakened — output/input ownership IS tensor/token/event byte-exact, but moving capacity from queued-only to queued+issued-not-complete changes ADMISSION TIMING even with `:398` present → relabel 1b "tensor/token/event byte-exact with stricter admission semantics" + add a saturated admission smoke to its gate; (MINOR) Step 2 needs a ZERO-CONSUMER retirement path (abandoned futures / admission timeout: retire by recording-stream query when no consumer `cudaStreamWaitEvent` was enqueued). Codex confirmed CONVERGED after these: input-lifetime owner + event point correct; Steps 2/3a/3b/4/5 sound. **v3** — after Round 3 (Codex `bxcg1gud1` + Opus). R3 fixes: (BLOCKER) async **input-tensor lifetime** was missed — the source input tensors (`ready`/`QueueItem::request.input` `:367`) the pack `index_copy_` (`:463`) READS are worker-owned; async return lets the worker free/reuse them before the GPU pack reads (the `:398` sync currently keeps them alive) → a **pending-dispatch owner** must retain all row inputs until a dispatcher-stream `dispatch_done` event completes; (MAJOR, correctness/liveness not perf) backpressure policy PICKED: **capacity tokens acquired at enqueue, released k-at-a-time only when the dispatch completion event completes** (early release → unbounded pending CUDA work / event+result lifetime growth / OOM / false admission timeouts) — same pending deque as the input-lifetime owner; (MINOR) Step 2 — the consumer-fence event is itself pending (retire original+fence only after `cudaEventQuery(fence)==success`), single-consumer topology confirmed. Step 1 re-split along the SAFE cut (resolves "too big"): **1a** measurement infra (poll-timing + util-denominator + worker-wait metric + async-ordering test harness + stream-order comments), sync UNCHANGED, byte-exact; **1b** ownership(5-field clone-before-completion-record)+input-lifetime-owner+completion-based-capacity-tokens WITH THE OLD SYNC STILL PRESENT (so byte-exact/safe); **1c** the small atomic async FLIP (remove/gate `:398`, fulfil futures before GPU completion). Steps now 0,1a,1b,1c,2,3a,3b,4,5. **v2** — after Round 2 (Codex `b65hbtjnm` + Opus). R2 fixes: (BLOCKER) splitting 1a/1b created a BROKEN intermediate (removing `:398` without the ownership fix + the async backpressure change can't be a standalone safe commit) → MERGED into one atomic **Step 1 "async dispatch"** (sync-removal + 5-field output-ownership + backpressure/capacity accounting + worker completion-wait timeout/metric + poll-timing + stream-order invariant); (BLOCKER) output audit must cover ALL 5 row tensors (enc_out/enc_len/cache_ch/cache_t/cache_ch_len), `.contiguous()` is NOT proof of ownership (no-op view for B=1 slices), and any clone must be enqueued BEFORE the completion event is RECORDED (`:436`), not merely before `set_value`; (BLOCKER) event-pool deferred retirement via `cudaEventQuery` alone is INSUFFICIENT (proves the recording stream reached it, not that consumer streams passed their `cudaStreamWaitEvent`) → retire behind CONSUMER-stream fence events (Step 2); (MAJOR) poll-mode timing can't use the `:436` completion event (cudaEventDisableTiming + post-unpack) → timing-enabled start/stop in a pending deque drained by query + fix the util denominator; (MAJOR) multi-dispatcher (Step 4) also shares `AOTIModelPackageLoader` handles in `loader_set_` → per-dispatcher loader handles or proven `steady_num_runners≥R`; (MAJOR) add a multi-chunk-per-session (2-3 continuation chunks) + concurrent + delayed-consumer correctness test to every async step's gate; note `scheduler_future_wait_ms` semantics SHIFT post-async (no longer "waited for GPU completion"). Steps renumbered 0,1,2,3a,3b,4,5. **v1** — after Round 1 (Codex `blfc5guzc` + Opus). R1 fixes: (BLOCKER) event-pool reuse race → split into Step 1c with deferred-retirement (CUDA-query/fence) reuse + its own stress gate; (BLOCKER) graph pointer-lifetime → Step 2a must prove fixed-address staging/preallocated length+output buffers + a slot/ring lifetime model (torch::full :467 + .contiguous() :521 allocate fresh today); (BLOCKER) multi-dispatcher → per-dispatcher scratch+stream AND a central batch-former so R dispatchers don't split B4 batches; (MAJOR) Step-0 telemetry sources corrected (`future_wait`/`enqueue_wait` are in `/stats`, NOT `/scheduler_telemetry`; `dispatcher_stream_util_pct` = GPU-work-fraction-on-dispatcher-timeline, NOT dispatcher saturation → read `dispatcher_cpu_pct` + queue/backlog + NVML too); (MAJOR) measurement circularity (gating the timing off removes `dispatcher_stream_util_pct`) → keep a low-overhead measurement-mode timing; (MAJOR) split Step 1 → 1a sync / 1b async-futures+output-ownership-audit / 1c event-pool; the `:398` removal is correct but the reason is single-stream ordering (made explicit + a delayed-worker-consumption test); confirmed steady T is fixed (SHIFT=16/PRE=9/25-frame chunk → graph key = bucket+shape+slot, K2/K3 padded to B4); adaptive knee bracketing + a quantified "moves-the-knee" noise threshold. **v0** — initial: activated the PHASE2-PLAN Tier-3/Tier-4 levers + the Step-1c clean-knee kill-gate off the nsys binding-inverted finding._

## Context
Cross-stream batching (BatchedSteadyScheduler B≤4) relieved the DRAM-BW floor and lifted the L40S native-runtime knee 36 → ≥64. The 2026-05-30 nsys re-profile @ N=96 shows the binding has **inverted**: the GPU is only ~74.5% busy (1.22× kernel overlap) and the server sheds via `steady scheduler future timeout` — the **single dispatcher saturates** while the GPU is starved (still 71% GEMM when busy, so the *next* wall after this is the B=4 steady GEMM again). This plan raises the knee by attacking the dispatch front-end — but its FIRST step is a no-profiler clean-knee measurement that must confirm the GPU is genuinely idle-able (the nsys idle is CUPTI-inflated + measured past the sustainable knee), because if the clean GPU is already saturated the dispatch levers are the wrong target. Hard constraint throughout: **P95 TTFS < 400ms**.

## Reference implementations
- **The capacity plan being extended:** `proj-2026-05-24-from-scratch-runtime/PHASE2-PLAN.md` (5-round paired-reviewed; v4 insight#4 pre-registered "~N=80-100 the single-thread dispatcher saturates → multi-dispatcher = deferred Tier-4"); `…/CHECKPOINT-scaling-above-36.md` §3 (lever tiers: Tier-3 steady CUDA-graph, Tier-4 multi-dispatcher) + §4 (the pre-registered GO/STOP kill-gate tree — Step 1c-B is the clean-knee arbiter this plan's Step 0 implements).
- **The evidence:** `proj-2026-05-30-2202/nsys-paired-verdict.md` (binding inverted), `codex-nsys-analysis.md`, `opus-nsys-analysis.md`.
- **The shipped finalize-graph precedent (Tier-3 template):** the Python `NEMOTRON_ENCODER_CUDAGRAPH_FINALIZE` fail-closed B=1×T graph (proj-2026-05-23-1731 finalize-graph-WIN) — collapsed ~1376 launches/finalize, byte-exact graph-on==graph-off, per-call savings compounded in a serialized queue. Same shape applies to the steady scheduler dispatch.
- **The graph NO-GO precedent (the risk Step 2a probes):** the DECODE grow-and-mask CUDA graph was rejected (proj-2026-05-23-1731 step-2 spike: max-B FULL_GRAPH crashed at B=1, grow-and-mask unsafe). The steady encoder is *fixed-shape per (B,bucket)* (unlike decode's growing T), so it is a candidate — but capturability of the AOTI run + the index_copy_ pack/unpack must be probed before building.
- No external (vLLM/SGLang) reference — bespoke single-process dispatch scheduler; the in-repo finalize-graph + batched-steady patterns are the precedent.

## Current state
All line numbers at git HEAD `44ed24f` (cold-start Phase 2 complete). Runtime: `proj-2026-05-24-from-scratch-runtime/runtime`.
- **`cpp/lib/scheduler/batched_steady_scheduler.cpp`** — SINGLE-dispatcher design:
  - `:136` `BatchedSteadyScheduler::start()` → `:141` `dispatcher_thread_ = std::thread([this]{ dispatcher_loop(); })` (exactly one dispatcher thread; one `dispatcher_stream_`).
  - `:260` `dispatcher_loop()` → `:266` `gather_batch()` → `:268` `dispatch_batch(batch)`.
  - `:353` `dispatch_batch(...)`: runs the batched AOTI steady, then a **per-dispatch telemetry block `:391-402`**: `cudaEventCreate(ev_start/ev_stop)` (`:391-392`), `cudaEventRecord` (`:393/:395`), **`cudaEventSynchronize(ev_stop)` (`:398` — BLOCKING host wait)**, `cudaEventElapsedTime` (`:400`), `cudaEventDestroy` (`:401-402`). The dispatcher BLOCKS on the GPU every dispatch purely for elapsed-time telemetry.
  - `:432` `cudaEventCreateWithFlags(&completion, cudaEventDisableTiming)` + `:436` record (per-row completion event, created per dispatch).
  - `:446` `batch[i]->promise.set_value(std::move(result))` — futures fulfilled AFTER the `:398` host sync.
  - `:374` / `:513` `cudaEventDestroy(item->request.producer_event)` (producer events destroyed per item; created per enqueue in session.cpp).
  - `:463-468` `scratch.{chunks,cache_ch,cache_t,cache_ch_len,length}.index_copy_(...)` = the D2D batched-cache PACK (the 160 GB / 163K-copy D2D the nsys flagged).
- **`cpp/lib/scheduler/batched_steady_scheduler.h`** — `:60` `DispatchResult::CompletionEvent` (RAII destroy of the per-row completion event); `:185-206` the single dispatcher thread / queue / stream members (the topology Step 3 would shard).
- **`cpp/lib/scheduler/steady_batch_primitive.h`** — `:520` row-unpack (contiguous per-row tensors from the batched output).
- **`cpp/lib/session/session.cpp`** — producer CUDA events created per scheduler enqueue (`~:1990`, `~:2336`); the future-wait the worker blocks on (`~:2994`); decode scalar D2H (`enc_len :446`, greedy `argmax().item() :1818`).
- **Telemetry sources (R1 correction — two different endpoints):** `/scheduler_telemetry` JSON has `counts.{dispatch_cycles,B1,B4,backlog_gt_bmax,dispatcher_exceptions}`, `dispatcher_cpu_pct`, `dispatcher_stream_util_pct`, `timers.{gather_wait_us,service_wait_us,cuda_run_us,worker_blocked_us}`, `queue_depth.{p50,p95,p99}`. **BUT `scheduler_future_wait_ms` / `scheduler_enqueue_wait_ms` are NOT here — they are in `/stats` / finalize timing (`stats_collector.cpp:141-143,265-266`)** — Step 0 must collect BOTH. **`dispatcher_stream_util_pct` = `dispatcher_stream_run_us / dispatcher_wall_us`, where `dispatcher_stream_run_us` comes ONLY from the AOTI timing event around `run_raw_prepacked` (`:393-400`)** — it EXCLUDES pack/unpack and is a "GPU-work-fraction-on-the-dispatcher-timeline," NOT a faithful dispatcher-saturation signal. The dispatcher-thread CPU saturation signal is `dispatcher_cpu_pct`. Gating the `:391-402` timing off (Step 1) removes the source of `dispatcher_stream_util_pct` → Step 1 must keep a low-overhead measurement-mode timing for the re-measures.
- **L40S box:** `34.214.169.199` (sm_89, current binary, `~/density`, `cpp/build_l40s_density`). On-box loadgen: `~/density/rt_loadgen.py` (realtime pacer) + `ec2_loadgen.py`. Local dev: 5090, `cpp/build_step10`.

## Rules
Shared rules from `proj-2026-05-24-from-scratch-runtime/PLAN_RULES.md` apply (venv/oracle/build conventions; AOTI is tolerance-equal, the bar is TOKEN-exact + EVENT/DELTA-exact vs finalize_ref — do NOT loosen to WER). Plus:

### Correctness (every code step)
- **Byte/token/event-EXACT gates, all must pass before commit:** `density_main … --mode b2-t1 --correctness-rows 4` (token_divergences=0, event_divergences=0, errors=0, dispatcher_exceptions=0); `--mode runtime-smoke` (0/0); compat oracle 8/8 event-for-event; STEADY shadow `STEADY_SHADOW_REPORT verdict=PASS`; perf-gate no regression. These are host-scheduling/graph changes — they MUST NOT alter numerics.
- A CUDA-graph step additionally requires **graph-ON == graph-OFF** (0 token/event divergence, tensor diff within the established AOTI tol) as an explicit sub-gate, fail-closed + env-gated default-off until validated.
- **The async-ordering gate (Steps 1, 2, 3b, 4):** b2-t1 + runtime-smoke are NOT sufficient for the async hazards. Add a **multi-chunk-per-session + concurrent + delayed-consumer test**: ≥2 long sessions running concurrently, ≥2-3 continuation chunks each, with artificially DELAYED worker consumption (a worker holds its result while the dispatcher processes ≥2 later batches) — must prove (a) a session's chunk N+1 cannot use cache before chunk N's cache update is GPU-ordered, and (b) no result row is corrupted by a later dispatch. This is the test that actually exercises async future fulfilment + the output-ownership clone + event-pool reuse. Pool-size-1 variant for Step 2. (5090 gate — the L40S b2-t1 forced-barrier deadlock means L40S stays single-stream b2-t1 / runtime-smoke / serving smoke.)
- **`scheduler_future_wait_ms` semantics SHIFT after Step 1:** pre-async it includes GPU-completion wait; post-async the wait moves to the worker-side completion-event/decode sync. Re-measures must read the worker-side completion-wait metric (added in Step 1) too, not just `future_wait`, or the lift will be mis-attributed.

### Measurement methodology (the clean-knee protocol — Step 0 + every re-measure)
- **NO CUPTI/nsys for the knee** — it inflated host/launch/event time + GPU-idle. Use the server's own telemetry + `nvidia-smi dmon -s u` (1 Hz) + the realtime loadgen's TTFS p50/p95/p99 + keep-up lag.
- **Collect from BOTH endpoints + NVML, as a table (R1 correction):** `/scheduler_telemetry` (`dispatcher_cpu_pct`, `dispatcher_stream_util_pct`, `queue_depth` p50/p95/p99, `backlog_gt_bmax`, `dispatcher_exceptions`) AND `/stats?last=…` (`scheduler_future_wait_ms`, `scheduler_enqueue_wait_ms`) AND `nvidia-smi dmon` GPU-util. **`dispatcher_stream_util_pct` is a GPU-work-fraction proxy (excludes pack/unpack), NOT dispatcher saturation** — never use it alone.
- **Confirmation criterion (the causal arbiter):** the GPU is genuinely idle-able (dispatch/host-bound, dispatch levers are the right target) iff at the clean knee: NVML util materially < saturation AND `future_wait`/`enqueue_wait` + `queue_depth`/`backlog_gt_bmax` rise AND (the dispatcher is the limiter, shown by `dispatcher_cpu_pct` approaching saturation OR `dispatcher_stream_util_pct` low while work backs up). STOP/PIVOT (GEMM/compute-bound → wrong target) iff NVML util ≈ saturated with low future/enqueue-wait growth.
- **"Moves the knee" threshold (makes the park/revert rule operational):** a lever counts as moving the knee only if the SLO-robust zero-error knee rises by **≥ +4 streams** beyond run-to-run noise (establish noise from 2-3 repeats at the baseline knee in Step 0). A lever that's correctness-clean but < +4 is parked (kept env-gated/off, not shipped as default), reason logged.
- Run at the highest **zero-error, in-budget (P95 TTFS < 400ms)** N. **Adaptive bracketing:** start {64,80,96}; if the knee lands on a boundary add {72,88}; if 96 passes extend {112,128}. Estimates are relative to the **clean** knee, NOT the N=96 nsys point.

### Safety / sequencing
- **P95 TTFS < 400ms is a HARD constraint.** Prefer zero-wait levers (event-pool, async-dispatch, graph-replay). Do NOT widen `batch_window_ms` as a knee lever (it spends TTFS budget) — window/lone stay at the production setpoint W=0/L=0.
- **Step 0 gates the whole plan.** If Step 0 says STOP/PIVOT, do NOT build the dispatch levers — report + route to batching-depth/model levers (a different plan).
- **Step 2a (graph capturability probe) gates Step 2b.** The prior decode grow-and-mask graph was a NO-GO — prove the steady path is capture-safe at fixed B=1 and B=4 before building the replay.
- **Each code step's acceptance INCLUDES the L40S knee re-measure** (Step-0 protocol). A lever that passes correctness but does NOT move the measured clean knee is reverted/parked (not shipped as dead complexity), and the reason logged.
- Env-gate every risky change default-OFF with a fallback; never `pkill -f` a pattern matching the launcher; clean up owned `/tmp/*/data/aotinductor*` after runs; do NOT `ec2_down` the L40S box (orchestrator handles it).
- L40S known artifact: the b2-t1 forced-barrier multi-stream case deadlocks on sm_89 (test-only B2ReusableBarrier) — use the 5090 b2-t1 6/6 as the correctness oracle; on L40S use single-stream b2-t1 + runtime-smoke + serving smoke.

## Steps

- [x] **0. CLEAN-KNEE KILL-GATE (mandatory, no code change — gates the whole plan)**
  On L40S `34.214.169.199` run the on-box realtime loadgen (`~/density/rt_loadgen.py`, realtime pacing, 0-error required) against the current binary (scheduler on, bg-warmup, CAP≥96, TS enc_first), with **adaptive bracketing**: start `--sweep 64,80,96`; add {72,88} if the knee lands on a boundary; extend {112,128} if 96 passes. For each N capture the FULL table from BOTH endpoints + NVML (per the Measurement-methodology rule): `/scheduler_telemetry` (`dispatcher_cpu_pct`, `dispatcher_stream_util_pct`, `queue_depth` p50/p95/p99, `backlog_gt_bmax`, `dispatcher_exceptions`) AND `/stats?last=…` (`scheduler_future_wait_ms`, `scheduler_enqueue_wait_ms`) AND `nvidia-smi dmon -s u` @1Hz over the steady window, plus TTFS p50/p95/p99, keep-up lag, ok/err. Also do 2-3 repeats at the baseline knee to establish the run-to-run noise band (for the +4-streams "moves-the-knee" threshold). Find the highest zero-error, P95-TTFS<400ms N = the **clean knee**. Apply the pre-registered DECISION (per the Measurement rule's causal arbiter): **GO** (→ Step 1) if NVML util materially < saturation ∧ `future_wait`/`enqueue_wait`+queue/backlog rise ∧ the dispatcher is the limiter (`dispatcher_cpu_pct` near-saturated OR stream-util low while work backs up); **STOP/PIVOT** (dispatch = wrong target → report, route to batching-depth/model) if NVML ≈ saturated with low wait-growth. Output: the clean knee N, the telemetry table, the noise band, and the GO/STOP verdict. NO code change (commit the measurement notes only).
  Key files: `proj-2026-05-31-1050/step0-clean-knee.md`

- [x] **1a. (gated Step 0 GO) Measurement infra — sync UNCHANGED (byte-exact, pure addition)**
  Land everything the async flip will need to be MEASURABLE, while the blocking `:398` sync stays in place (so this commit is behaviorally identical / byte-exact): (i) a **poll-mode timing path** — gate the `:391-402` timing behind `NEMOTRON_WS_DISPATCH_TIMING` with a `=poll` option that pushes timing-ENABLED `ev_start/ev_stop` to a **pending deque drained by non-blocking `cudaEventQuery`** (NOT the `:436` `cudaEventDisableTiming` completion event); (ii) fix the `dispatcher_stream_util_pct` DENOMINATOR so it stays meaningful once dispatcher wall-time no longer includes the GPU wait; (iii) add a **worker-side completion-wait timeout + metric** field (session.cpp `:2380`) so the wait `future_wait` will no longer capture is recorded; (iv) build the **async-ordering test harness** (multi-chunk-per-session + concurrent + delayed-consumer, per Rules) and confirm it PASSES with the current sync (baseline); (v) add the **stream-order invariant** comment+assert (pack`:463`→run`:394`→unpack`:405`→next-pack ordered on the single `dispatcher_stream_`). With `=poll` default and `:398` still firing, ALL gates must be byte-exact unchanged. Validate: full suite (b2-t1 6/6 0/0, runtime-smoke 0/0, oracle 8/8, shadow PASS, perf-gate) — no behavior change.
  Key files: `cpp/lib/scheduler/batched_steady_scheduler.{cpp,h}`, `cpp/lib/session/session.{cpp,h}`

- [ ] **1b. (gated 1a) Ownership + input-lifetime + capacity tokens — OLD SYNC STILL PRESENT (tensor/token/event byte-exact; stricter admission timing)**
  Prepare every async-safety invariant WHILE the `:398` sync still serializes (the ownership/lifetime parts (i,ii) are behavioral no-ops → tensor/token/event byte-exact; the capacity-token change (iii) is tensor-exact but tightens admission timing — see (iii); this de-risks the flip): (i) **OUTPUT ownership — all 5 row tensors** (`enc_out`,`enc_len`,`cache_ch`,`cache_t`,`cache_ch_len`; the recurrent CACHE is held across to the session's next chunk → highest-risk). `.contiguous()` is NOT proof of ownership (no-op view for B=1 slices) — audit each; if any aliases the persistent output scratch, **clone it and enqueue the clone BEFORE the completion event is RECORDED (`:436`)**. (ii) **Input-lifetime owner (R3 BLOCKER):** a per-dispatch pending record that RETAINS all row input tensors (`ready`/`QueueItem::request.input` `:367`, read by the pack `index_copy_` `:463`) until a dispatcher-stream **`dispatch_done` event** (recorded after pack→run→unpack→output-clones) completes — so async return can't let the worker free/reuse its chunk+cache before the GPU pack reads it. (iii) **Completion-based capacity tokens (R3 MAJOR):** acquire a capacity token at enqueue (`:119`); do NOT release on `gather_batch` pop (`:309`); release k tokens + notify only when the dispatch `dispatch_done` event queries complete — `queue_capacity` then bounds queued + issued-but-not-completed (prevents unbounded in-flight CUDA work / OOM / false admission timeouts under async). (ii)+(iii) share ONE pending-dispatch deque. **PROGRESS-DRIVING retirement (R4 BLOCKER):** the deque MUST be drained/queried from progress-making points — in the enqueue capacity predicate, before any dispatcher idle wait (`gather_batch` `:301`), via a timed poll / small reaper, and a full drain+sync on `close()` before dropping input owners — so a token freed by CUDA completion always wakes a `cv_capacity_` waiter; NEVER an indefinite `cv_`/`cv_capacity_` wait while all capacity is pending on completion. NOTE this is **tensor/token/event byte-exact but has STRICTER admission timing** than today (queued+issued-not-complete vs queued-only) even with the sync present — so it is NOT a pure no-op. Validate: full suite (tensor/token/event unchanged) + **a saturated admission smoke** (admission cap-hits/timeouts behave correctly under sustained over-offer) + the async-ordering harness, all with the sync still on.
  Key files: `cpp/lib/scheduler/batched_steady_scheduler.{cpp,h}`, `cpp/lib/scheduler/steady_batch_primitive.h`, `cpp/lib/session/session.{cpp,h}`

- [ ] **1c. (gated 1b) The atomic async FLIP — remove the blocking sync**
  Now small + isolated: gate the blocking `cudaEventSynchronize(:398)` OFF (default), and fulfil `promise.set_value` (`:446`) WITHOUT the preceding host GPU-wait — the worker's `cudaStreamWaitEvent` on the completion event (`:2380`) + the 1b input-lifetime owner + capacity tokens are now the safety net. With the sync gone, `dispatch_done` completes asynchronously, so **the 1b progress-driving pending-deque drain (timed wait / reaper / enqueue-predicate drain) becomes load-bearing** — verify under this step that an idle queue with in-flight dispatches still releases capacity tokens + input owners promptly (no `cv_capacity_` stall). This is the only behavior-changing commit. Validate: full correctness suite + the **async-ordering gate** (multi-chunk-per-session + concurrent + delayed-consumer — now load-bearing) + L40S knee re-measure (this is where the lift shows; park if <+4).
  Key files: `cpp/lib/scheduler/batched_steady_scheduler.{cpp,h}`, `cpp/lib/session/session.{cpp,h}`

- [ ] **2. (gated 1c) CUDA event pooling with consumer-fence deferred retirement**
  Replace per-dispatch/per-enqueue `cudaEventCreate*`/`cudaEventDestroy` (`:391/:401/:432`, `:374/:513`, session.cpp producer events `~:1990/:2336` — ~270K/min) with a thread-safe reusable pool. RETIREMENT FIX (R2 BLOCKER): `cudaEventQuery` alone is INSUFFICIENT — it proves the RECORDING stream reached the event, not that CONSUMER streams passed their `cudaStreamWaitEvent`. Retire behind **consumer-stream fence events**: a producer event (one consumer: `dispatcher_stream_` `:370`) is reusable only after a fence recorded on `dispatcher_stream_` past the wait queries complete; a completion event (one consumer: session `ctx.stream` `:2380`) only after a fence on that worker stream past its wait completes. **The fence event itself is pending** — return BOTH the original event and its fence to their pools only after `cudaEventQuery(fence)==cudaSuccess` (R3). (Single-consumer topology confirmed; if a future event ever gets ≥2 consumer streams, one fence per consumer, retire after all.) Timing-only start/stop events MAY use stop-event query (no cross-stream consumer). **ZERO-CONSUMER retirement path (R4):** for an event where NO consumer `cudaStreamWaitEvent` was ever enqueued (abandoned future / admission timeout / dropped dispatch), retire by the RECORDING-stream completion query (a consumer fence will never arrive) — so pooled events can't leak on the error/timeout paths. Name the exact call sites: the dispatcher-fault drain (`:513` `set_item_exception` / producer-event destroy), scheduler `close()`, and future/`DispatchResult` RAII destruction (R5) — all must return their pooled events via the recording-stream-query path. Pool producer events per-lane, completion/timing per-scheduler. ADVERSARIAL STRESS GATE: **pool size 1 + delayed consumers** + the concurrent path (5090). Validate: full suite + the async-ordering gate + the pool-size-1 stress + L40S knee re-measure.
  Key files: `cpp/lib/scheduler/batched_steady_scheduler.{cpp,h}`, `cpp/lib/session/session.{cpp,h}`

- [ ] **3a. PROBE: steady-path CUDA-graph capturability + BUFFER LIFETIME (gates 3b)**
  Read-only/spike. CUDA graph replay PINS input/output device ADDRESSES, so capturability is necessary but not sufficient — the probe must prove a FIXED-ADDRESS buffer model. Check: (i) does `AOTIModelPackageLoader.run_raw_prepacked` (`steady_batch_primitive.h:283-289`) permit stream capture (no internal host syncs / `cudaMalloc` / disallowed ops)? (ii) the `index_copy_` pack (`:463-468`) writes into the persistent scratch (fixed address ✓) — BUT `torch::full({1}, …)` for `length` (`:467`) ALLOCATES fresh each call, and row-unpack `.contiguous()` (`steady_batch_primitive.h:521-537`) allocates fresh OUTPUTS each call → these break replay. (iii) Define the fix: pre-allocated fixed-address length buffer + pre-allocated fixed-address output buffers + a **slot/ring lifetime model** so a replay cannot overwrite an output slot before the consuming worker's decode has finished with it (how many in-flight slots, how recycled). Confirm steady **T is FIXED** (`SHIFT=16`/`PRE=9`/25-frame continuation chunk per `session.cpp:52-53,2969-2980`) so the graph key = `bucket + chunk/cache shape + slot` (K=2/3 pad to the B4 shape; `k` is host metadata). Spike a minimal capture+replay of ONE (B,bucket) geometry on fixed buffers; compare replay vs eager (token/event/tensor). Report GO/NO-GO per (B,bucket) + the exact blocker + the buffer-lifetime design. If NO-GO, skip 3b → Step 4.
  Key files: `proj-2026-05-31-1050/step2a-graph-probe.md`, a throwaway spike under `runtime/` (probe only)

- [ ] **3b. (gated 3a GO) Steady CUDA graph per (bucket,slot) on fixed buffers**
  Implement the 3a buffer-lifetime model (fixed-address staging in/length/out + slot ring), then capture/replay one CUDA graph per safe (bucket) shape (B4 shape; K<4 pads), fail-closed + env-gated `NEMOTRON_WS_STEADY_CUDAGRAPH` default-**off**. Replay collapses the per-dispatch launch storm (nsys ~7M launches / 69% API) with ZERO added batch-wait (preserves P95). Eager path stays as fallback for any non-captured shape. Validate: the **graph-ON==graph-OFF** sub-gate (0 token/event div, tensor within AOTI tol) + full suite + the async-ordering gate + a slot-recycle stress + L40S knee re-measure. Flip default-on only if knee improves (≥+4) AND all gates green; else ship opt-in.
  Key files: `cpp/lib/scheduler/batched_steady_scheduler.{cpp,h}`, `cpp/lib/scheduler/steady_batch_primitive.h`

- [ ] **4. (conditional — only if dispatch STILL binds after 1+3b) Multi-dispatcher, capped R=2**
  ONLY if re-measures still show dispatch-bound (`dispatcher_cpu_pct` saturated and/or persistent future-timeouts with NVML < sat at the knee). Add a FIXED pool of R=2 dispatcher workers, each with its OWN `dispatcher_stream_` AND its OWN per-bucket scratch (`scratch_[bucket]` is a shared member today — 2 dispatchers on 2 streams sharing it corrupts the pack: per-dispatcher scratch mandatory, `:479-501`/`.h:185-206`) AND its OWN `AOTIModelPackageLoader` handles (R2: the dispatchers also share the `loader_set_` loader instances — concurrent same-bucket `loader.run` is unsafe unless each dispatcher has its own handle OR `steady_num_runners≥R` is proven safe; `steady_batch_primitive.h:283`). B-FILL PRESERVATION (R1 BLOCKER): do NOT let R dispatchers race `gather_batch` (that SPLITS would-be-B4 batches → worse fill); use ONE locked **central batch-former** forming full B4 batches that hands completed batches to the R worker+stream+scratch+loader slots — only the GPU-dispatch is parallelized, fill preserved. **Cap R=2** (more → GEMM contention). SUBSTITUTE for 1/3b headroom — last, measured-necessary only. Validate: full suite + the async-ordering gate (esp. no double-dispatch / stale-gen / scratch+loader corruption) + L40S knee re-measure; park if < +4.
  Key files: `cpp/lib/scheduler/batched_steady_scheduler.{cpp,h}`, `cpp/lib/scheduler/steady_batch_primitive.h`

- [ ] **5. FINAL L40S knee sweep + capacity/funding update + PHASE2-PLAN v6**
  Bracket the new SLO-robust knee at N∈{72,80,88,96,112,128} (PHASE2-PLAN v4 follow-up #7) on the L40S: report knee, TTFS p50/p95/p99, keep-up lag, the streams/box multiplier vs S_py≈20, and confirm P95 TTFS < 400ms holds. State whether the binding moved back to the B=4 steady GEMM (the next-after-dispatch wall) or elsewhere (re-check `dispatcher_stream_util_pct` + NVML). Write the v6 revision note into `proj-2026-05-24-from-scratch-runtime/PHASE2-PLAN.md` recording which Tier-3/Tier-4 levers landed + the realized knee, and finalize this plan's progress. Measurement/doc step.
  Key files: `proj-2026-05-31-1050/step4-final-knee.md`, `proj-2026-05-24-from-scratch-runtime/PHASE2-PLAN.md`

## Progress
| # | Step | Status | Commit | Notes |
|---|------|--------|--------|-------|
| 0 | Clean-knee kill-gate (no build) | done | 07dc9ab | gates whole plan; GO iff NVML<sat + future/enqueue-wait rising + dispatcher the limiter (cpu_pct/stream); both endpoints+NVML; adaptive bracket; noise band | **VERDICT: GO (paired Codex+Opus). Clean knee N≈56-60 (cliff 64, fail ≥72). Cliff=:398 CPU↔GPU ping-pong NOT GEMM-sat (NVML 87-90%<95, disp-cpu 58-83% unpegged, queue pinned, backlog rising). HONEST: clean GPU ceiling ~90% (vs nsys-inflated 74.5%) → realizable ~+4-8 (58→~64-68); +4 bar borderline; Steps 3/4 diminishing. step0-clean-knee.md** |
| 1a | Measurement infra (sync UNCHANGED, byte-exact) | done | 01d17db | gated Step0 GO; poll-timing deque + util denominator + worker-wait metric + async-ordering test harness (baseline) + stream-order invariant comment |
| 1b | Ownership + input-lifetime + capacity tokens (OLD SYNC PRESENT; tensor/token/event byte-exact, stricter admission) | pending | — | gated 1a; 5-field clone-before-completion-record; pending-dispatch input-lifetime owner; PROGRESS-DRIVING completion-based capacity tokens (drain in enqueue-pred/before-idle/reaper/close); + saturated admission smoke |
| 1c | Atomic async FLIP (remove :398 sync) | pending | — | gated 1b; only behavior-changing commit; 1b reaper load-bearing (idle-queue token release); async-ordering gate; L40S knee lift shows here |
| 2 | Event pool w/ consumer-fence deferred retirement | pending | — | gated 1c; fence on consumer stream + fence-event itself pending + zero-consumer (recording-stream-query) path; pool-size-1 + async-ordering stress |
| 3a | PROBE graph capturability + BUFFER LIFETIME | pending | — | gates 3b; graph pins addresses — torch::full :467 + .contiguous() :521 alloc fresh; need fixed buffers + slot/ring; T fixed |
| 3b | Steady CUDA graph per (bucket,slot) on fixed buffers | pending | — | gated 3a GO; collapse 7M launches; graph-ON==OFF gate; env default-off; slot-recycle stress |
| 4 | Multi-dispatcher capped R=2 (per-disp scratch+stream+loader, central batch-former) | pending | — | conditional/substitute; per-dispatcher scratch+loader-handles mandatory; central batch-former preserves B-fill; park if <+4 |
| 5 | Final L40S knee sweep + v6 update | pending | — | adaptive N{64..128}; multiplier vs S_py≈20; confirm P95<400ms; PHASE2-PLAN v6 |
